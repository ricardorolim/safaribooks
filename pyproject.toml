[project]
name = "safaribooks"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "browser-cookie3>=0.20.1",
]

[project.scripts]
safaribooks = "safaribooks.__main__:main"

[tool.uv]
package = true
